# Database
DB_HOST=localhost
DB_USER=smarthealth
DB_PASSWORD=Test12####
DB_DATABASE=smarthealth

# JWT / security
SECRET_KEY=smarthealth-secret-2026
JWT_SECRET_KEY=smarthealth-jwt-secret-2026

# --- Email (pick ONE approach) ---

# Option A: Local dev without real email (default when SMTP is unset)
# Verification codes are logged in the backend console and returned in API responses.
# EMAIL_DEV_MODE=true
# SMTP_ENABLED=false

# Option B: Gmail SMTP (requires a Google App Password — not your normal password)
# https://myaccount.google.com/apppasswords
SMTP_ENABLED=true
EMAIL_DEV_MODE=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USER=laziz.youssef@gmail.com
SMTP_PASSWORD=ahfa mfbw sckr uhau
SMTP_FROM=laziz.youssef@gmail.com

# --- Social login (leave blank to disable a provider) ---
# Must match the frontend's VITE_GOOGLE_CLIENT_ID / VITE_APPLE_CLIENT_ID.
GOOGLE_CLIENT_ID=
APPLE_CLIENT_ID=
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
